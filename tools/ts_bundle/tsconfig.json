{
  "compilerOptions": {
    "target": "ES2020",               // Современная версия JS
    "module": "ES6",               // Для поддержки модулей (Webpack разберётся)
    "strict": true,                // Строгая проверка типов
    "esModuleInterop": true,       // Для совместимости с CommonJS
    "skipLibCheck": true,          // Ускоряет компиляцию
    "outDir": "./dist",            // Папка для скомпилированных файлов (не обязательно, т.к. Webpack сам соберёт)
    "rootDir": ".",            // Папка с исходниками
    "moduleResolution": "node"     // Для правильного разрешения модулей
  },
  "include": ["./stroppy.pb.ts"],      // Где искать .ts файлы
  "exclude": ["node_modules"]      // Что игнорировать
}